% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/plotUpset-methods.R
\name{plotUpset}
\alias{plotUpset}
\alias{plotUpset,DataFrame-method}
\alias{plotUpset,data.frame-method}
\alias{plotUpset,list-method}
\alias{plotUpset,matrix-method}
\title{UpSet plot}
\usage{
plotUpset(object, ...)

\S4method{plotUpset}{DataFrame}(object, ...)

\S4method{plotUpset}{data.frame}(object, ...)

\S4method{plotUpset}{list}(object, ...)

\S4method{plotUpset}{matrix}(
  object,
  minSize = 0L,
  maxSize = Inf,
  nIntersections = 40L,
  orderBySize = c(intersections = TRUE, sets = TRUE),
  labels = list(title = NULL, subtitle = NULL)
)
}
\arguments{
\item{object}{Object.}

\item{...}{Additional arguments.}

\item{minSize, maxSize}{Non-negative \code{integer(1)} or \code{Inf}.
Minimal/maximal number of observations in an intersection for it to be
included. Defaults to all observations. Note that \code{maxSize} must be
greater than \code{minSize}.}

\item{nIntersections}{\code{integer(1)} or \code{Inf}.
Maximum number of intersections to plot.
Set \code{Inf} to plot all intersections.}

\item{orderBySize}{\code{logical}.
Whether to order main bar plot and/or intersection matrix by set size.
\itemize{
\item \code{"intersection"}: refers to main intersection bar plot.
\item \code{"sets"}: refers to set intersection matrix, shown as connected dots.
When \code{TRUE}, orders by the set size (see plot to the left).
}

Can pass in \code{TRUE}/\code{FALSE} boolean flag and both \code{"intersection"} and
\code{"sets"} settings will inherit.}

\item{labels}{\code{list}.
ggplot2 labels.
See \code{ggplot2::labs()} for details.}
}
\value{
Plot.
}
\description{
UpSet plot
}
\details{
S4 wrapper for \code{ComplexUpset::upset()} with improved default aesthetics.
}
\note{
Updated 2022-04-05.
}
\examples{
## list ====
list <- list(
    "aaa" = c("a", "b", "c", "d", "e", "f"),
    "bbb" = c("b", "c", "d", "e", "f", "g"),
    "ccc" = c("c", "d", "e", "f", "g", "h"),
    "ddd" = c("d", "e", "f", "g", "h", "i")
)
print(list)
plotUpset(list)

## matrix ====
mat <- AcidBase::intersectionMatrix(list)
print(mat)
plotUpset(mat)
}
\seealso{
\itemize{
\item \code{intersectionMatrix()}.
\item \code{ComplexUpset::upset()}.
\item \code{ComplexUpset::upset_themes}.
\item UpSetR package (legacy approach).
}
}
